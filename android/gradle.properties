# This file is generated by Flutter tool.
# As opposed to this file, the settings.gradle should NOT be checked into
# version control, as it is generated specifically for local development.

# Specifies the JVM arguments used for the daemon process.
# The setting is particularly useful for tweaking memory settings.
org.gradle.jvmargs=-Xmx4G

# Enable Gradle parallel builds for faster compilation
org.gradle.parallel=true

# Enable Gradle caching
org.gradle.caching=true

# AndroidX package structure
android.useAndroidX=true

# Kotlin code style for this project: "official" or "obsolete":
kotlin.code.style=official

# Enables namespacing of each library's R class
android.nonTransitiveRClass=true

# Enable Gradle configuration on demand
org.gradle.configureondemand=true
